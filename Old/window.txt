
/*
    // Create an SFML texture and load the image into it
    sf::Texture texture;
    texture.loadFromImage(image);

    // Create an SFML sprite and set its texture
    sf::Sprite sprite;
    sprite.setTexture(texture);

    // Create an SFML window
    sf::RenderWindow window(sf::VideoMode(env.n, env.n), "SFML Image Display");

    // Main loop
    while (window.isOpen()) {
        // Process events
        sf::Event event;
        while (window.pollEvent(event)) {
            if (event.type == sf::Event::Closed) {
                window.close();
            }
        }

        // Clear the window
        window.clear();

        // Draw the sprite
        window.draw(sprite);

        // Display the contents of the window
        window.display();
    }

    // Cleanup
    delete[] pixels;
*/
